import styled from 'styled-components';

export const Portrait = styled.img`
  width: 130px;
  height: 130px;
  border-radius: 50%;
  object-fit: cover;
`;

export const Name = styled.div`
  font-size: 18px;
  font-weight: bold;
  margin: 6px 0 24px;
`;
